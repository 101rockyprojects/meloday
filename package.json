{
  "name": "meloday",
  "description": "A melody each day",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev --port 4000",
    "build": "vite build",
    "preview": "vite preview --port 4000",
    "deploy": "npm run build",
    "cf:pages:dev": "npm run build && wrangler pages dev .svelte-kit/cloudflare",
    "cf:pages:deploy": "npm run build && wrangler pages deploy .svelte-kit/cloudflare"
  },
  "devDependencies": {
    "@sveltejs/adapter-cloudflare": "^6.0.0",
    "@sveltejs/kit": "^2.7.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tailwindcss/forms": "^0.5.9",
    "autoprefixer": "^10.4.20",
    "svelte": "^5.1.3",
    "tailwindcss": "^3.4.15",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "@justinribeiro/lite-youtube": "^1.8.2"
  }
}
